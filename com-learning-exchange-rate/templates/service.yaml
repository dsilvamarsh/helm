apiVersion: v1
kind: Service
metadata:
  name: {{ include "com-learning-exchange-rate.fullname" . }}
  labels:
    {{- include "com-learning-exchange-rate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "com-learning-exchange-rate.selectorLabels" . | nindent 4 }}
