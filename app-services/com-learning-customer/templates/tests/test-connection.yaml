apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "com-learning-customer.fullname" . }}-test-connection"
  labels:
    {{- include "com-learning-customer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "com-learning-customer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
