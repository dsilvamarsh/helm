apiVersion: v1
kind: Service
metadata:
  name: {{ include "com-learning-customer.fullname" . }}
  labels:
    {{- include "com-learning-customer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "com-learning-customer.selectorLabels" . | nindent 4 }}
