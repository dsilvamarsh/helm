apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "com-bank-core-currency.fullname" . }}-test-connection"
  labels:
    {{- include "com-bank-core-currency.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "com-bank-core-currency.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
