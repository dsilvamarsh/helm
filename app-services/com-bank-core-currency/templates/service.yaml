apiVersion: v1
kind: Service
metadata:
  name: {{ include "com-bank-core-currency.fullname" . }}
  labels:
    {{- include "com-bank-core-currency.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "com-bank-core-currency.selectorLabels" . | nindent 4 }}
