apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "com-learning-transaction-watcher.fullname" . }}-test-connection"
  labels:
    {{- include "com-learning-transaction-watcher.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "com-learning-transaction-watcher.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
